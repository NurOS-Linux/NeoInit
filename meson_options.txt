option('systemd_compat', type : 'boolean', value : true,
  description : 'Build systemd unit file compatibility parser')

option('runit_compat', type : 'boolean', value : false,
  description : 'Build runit service directory compatibility parser')

option('yaml_compat', type : 'boolean', value : true,
  description : 'Build legacy YAML unit file compatibility parser (TOML is the native format)')

option('default_path', type : 'string', value : '',
  description : 'Default PATH for services if not set in environment (empty means use system default)')

option('services_dir', type : 'string', value : '/etc/raesir/services',
  description : 'Directory to scan for service definitions')

option('container_detection', type : 'boolean', value : true,
  description : 'Enable detection of container environments to skip reboot/umount')
